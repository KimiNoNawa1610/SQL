select * from Items;
--float and double will store a larger number using less space but it comes at the cost of precision. 
--Float will have precision issue after 7 digits. 
--Double will have precision issue after 15 digits.